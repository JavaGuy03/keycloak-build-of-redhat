parent=keycloak
import=common/keycloak

styles=css/styles.css

# Trang login đã được viết cứng bằng tiếng Việt trong login.ftl (không dùng message bundle),
# nên không cần locale selector. Giữ dòng dưới để Keycloak không báo lỗi cấu hình locale.
locales=vi
