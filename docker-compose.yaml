services:
  postgres:
    image: postgres:16-alpine
    container_name: keycloak_postgres
    environment:
      POSTGRES_DB: keycloak
      POSTGRES_USER: keycloak
      POSTGRES_PASSWORD: ${KC_DB_PASSWORD:?Set KC_DB_PASSWORD in the runtime environment}
    volumes:
      - pgdata:/var/lib/postgresql/data
    networks:
      - kc-net

  keycloak:
    image: ${KEYCLOAK_IMAGE:-keycloak-custom:dev}
    build: .
    container_name: keycloak_server
    environment:
      KC_DB: postgres
      KC_DB_URL: jdbc:postgresql://postgres:5432/keycloak
      KC_DB_USERNAME: keycloak
      KC_DB_PASSWORD: ${KC_DB_PASSWORD:?Set KC_DB_PASSWORD in the runtime environment}
      KC_HTTP_ENABLED: "true"
      KC_HOSTNAME_STRICT: "false"
      KEYCLOAK_ADMIN: admin
      KEYCLOAK_ADMIN_PASSWORD: ${KEYCLOAK_ADMIN_PASSWORD:?Set KEYCLOAK_ADMIN_PASSWORD in the runtime environment}
    ports:
      - "8085:8080"
    depends_on:
      - postgres
    networks:
      - kc-net
    command: ["start", "--optimized", "--http-enabled=true"]

volumes:
  pgdata:

networks:
  kc-net:
